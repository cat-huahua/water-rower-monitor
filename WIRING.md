# Water Rower Monitor - 接线图 & 配置指南 (Waveshare ESP32-S3-Touch-LCD-2.8)

> 换板后**几乎不用接线**：屏幕、触摸、喇叭、电池管理全部板载。
> 唯一要接的是飞轮传感器的 3 根线。

## 板载已连接 (不需要接线)

这些引脚在 PCB 上已经走好线，`config.h` 里的定义只是告诉驱动它们在哪，
改这些值不会改变硬件连接，只会让驱动失效。

| 功能 | 芯片 | 引脚 |
|------|------|------|
| LCD | ST7789 240×320 SPI | SCK=40, MOSI=45, CS=42, DC=41, RST=39, BL=5 |
| 触摸 | CST328 (I2C1, 地址 0x1A) | SDA=1, SCL=3, RST=2, INT=4 |
| 音频 | PCM5101 I2S DAC + 功放 | BCK=48, LRCK=38, DIN=47 |
| IMU / RTC | QMI8658 / PCF85063 (I2C0) | SCL=10, SDA=11 (本固件未使用) |
| TF 卡 | SPI | CS=21, SCK=14, MOSI=17, MISO=16 (本固件未使用) |
| 电池 | 充电管理 + 电量检测 | ADC=8, 控制=7, 按键=6 (本固件未使用) |

## 12PIN 排针 (唯一对外接口)

```
GND · VBus(5V) · 3V3 · D-(IO19) · D+(IO20) · SCL(IO10)
SDA(IO11) · TXD(IO43) · RXD(IO44) · IO18 · IO15 · GND
```

> 实际排列以板上丝印为准。
> **可自由使用的只有 IO18 和 IO15** —— 其余都被 USB、板载 I2C、UART0 占用。
> 本固件用 **IO18** 接传感器，IO15 留空备用。

## 需要接线的部分: 飞轮传感器

原机传感器 (Optek OPB365T55 槽型光耦, 已测试确认)

```
线缆颜色       →   ESP32-S3-Touch-LCD-2.8       注意
────────────────────────────────────────────────────────
红 (VCC)       →   12PIN 的 VBus                !! 必须 5V, 3.3V 不够 !!
绿 (GND)       →   12PIN 的 GND
蓝 (SIG)       →   12PIN 的 IO18                数字信号, 遮光=LOW
```

```
   ┌─ 原机传感器 (3线) ─┐        ┌── ESP32-S3-Touch-LCD-2.8 ──┐
   │                    │        │                            │
   │  红 VCC  ──────────┼────────┼──→ VBus (5V)               │
   │  绿 GND  ──────────┼────────┼──→ GND                     │
   │  蓝 SIG  ──────────┼────────┼──→ IO18                    │
   └────────────────────┘        │                            │
                                 │  USB-C ←──── 充电宝 (外挂)  │
                                 └────────────────────────────┘
                                        ↑ 同一路 5V 也喂给传感器
```

> 传感器内部有比较器 + IR LED 限流电阻，不需要外接电阻。
> 光电管是开集电极输出，固件用 `INPUT_PULLUP` 上拉到 3.3V，
> 所以 5V 供电不会把 5V 灌进 GPIO。

### 传感器的 5V 从哪来

`VBus` 就是 USB-C 进来的 5V。**用外置 USB 充电宝供电时，传感器自动就有 5V**，
不需要升压板，也不需要换传感器。

只有一种情况会缺 5V：不用充电宝，改接 MX1.25 锂电池。那时 VBus 没电，
要么加 3.7V→5V 升压小板，要么改用霍尔传感器
(`config.h` 里换成 `SENSOR_TYPE_HALL`，同样接 IO18，3.3V 可用)。

## 电源：外置 USB 充电宝

```
2节18650 充电宝 (CK 18650-1234S-NP 之类) ──USB-A→C 短线──→ 板子 USB-C
         ↑ 魔术贴/扎带固定在划船机管子上
```

充电宝放在外壳**外面**，用魔术贴或扎带绑在划船机管子上，线从外壳底边的
USB-C 开口进来。这样：

- 传感器的 5V 自动解决（VBus = USB 5V）
- 外壳不用装电池，整机只有 **19.6mm 厚**
- 换电池、充电不用拆壳
- 充电宝自带保护板和充电电路，不用管板载的那套

建议用**直角 USB-C 线**，减小外壳开口处的受力。

### ⚠️ 要实测：充电宝的自动关断

很多充电宝在输出电流低于 50-70mA 时会自动断电省电。本机约 150-250mA，
正常不会断，但**必须实测**：装好后把亮度调到最低，放着跑 15-20 分钟看会不会黑屏。

真的会断的话，在 12PIN 的 `VBus` 和 `GND` 之间并一个 100Ω 1W 电阻
（约 50mA 假负载），代价是续航变短。

另外确认充电宝输出是 5V/1A 还是 2A —— 1A 够用，但开机 WiFi 扫描有电流尖峰，
劣质 1A 板子可能掉压重启。

### 备选：MX1.25 锂电池（本方案不用）

如果哪天不用充电宝、改接板载电池座：

- 必须是 **3.7V 单节 (1S)**，4.2V 满电
- **7.4V 是两节串联 (2S)，接上去会烧掉板载充电电路和主控**
- **插之前用万用表量极性** —— JST 1.25 电池的红黑线序各家不一样，接反同样烧板
- 那时传感器就没 5V 了，要另想办法（见上面「传感器的 5V 从哪来」）

## 操作方式 (全触摸，没有物理按键)

板上只有 BOOT 和 RESET 两个键，跟操作无关。所有交互都在屏幕上：

| 界面 | 操作 |
|------|------|
| 待机 | `START` 开始 · `HISTORY` 看历史 · `- / +` 调亮度 |
| 待机 | **长按顶部标题栏 3 秒** → 进入管理员 |
| 选人 | 点名字选中，再点 `START`；上下滑动翻页 |
| 划船中 | `PAUSE` 暂停 |
| 暂停 | `RESUME` 继续 · `FINISH` 结束并上传 |
| 历史 | `PREV` / `NEXT` 翻页，或上下滑动 |
| 管理员密码 | 屏幕数字键盘 0-9，`DEL` 退格，`ESC` 取消 |

## 需要修改的变量

### config.h — 必须改

| 变量 | 说明 | 示例 |
|------|------|------|
| `WIFI_SSIDS` / `WIFI_PASSWORDS` | WiFi 列表（自动选信号最强的） | `{"MyWiFi"}` |
| `NAS_HOST` | TrueNAS 地址 | `"192.168.1.100"` |
| `NAS_API_KEY` | TrueNAS API Key | `"1-xxxx..."` |
| `NAS_PATH` | 存放训练记录的目录 | `"/mnt/pool/water_rower_records"` |
| `USER_COUNT` / `USER_NAMES` | 使用者名单 | `{"Alice", "Bob"}` |
| `MACHINE_SN` | 机器编号 | `"132224"` |

### config.h — 校准 / 手感

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `METERS_PER_PULSE` | 每个脉冲行进米数 — **标定量**，也可在管理员菜单里调 | `0.00278f` |
| `PULSES_PER_REV` | 每转脉冲数 (60 齿) | `60` |
| `BLOCKER_MIN_PULSE_US` | 脉冲最小间隔 (防抖) | `300` |
| `STROKE_MIN_MS` | 两桨最小间隔 (ms, ~100SPM 上限) | `600` |
| `STROKE_HYST` | drive/recovery 翻转阈值 (比例) | `0.15f` |
| `TONE_VOLUME` | 提示音音量 0.0-1.0（板上还有音量旋钮） | `0.35f` |

### config.h — 触摸方向 (如果点不准才需要动)

上电后如果发现点击位置和实际按钮**镜像**或**XY 反了**，翻转对应开关即可，
其它代码都不用改：

| 变量 | 症状 |
|------|------|
| `TOUCH_MIRROR_X` | 左右反了 |
| `TOUCH_MIRROR_Y` | 上下反了 |
| `TOUCH_SWAP_XY` | 横竖轴反了 |

### enclosure.scad — 根据实物调

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `tube_od` | 原机金属管外径 | `18mm` (已确认) |
| `pcb_w` / `pcb_l` | PCB 尺寸 | `49.90 / 69.00mm` (官方图纸) |
| `glass_w` / `glass_l` | 触摸玻璃外形，**比 PCB 大** | `50.54 / 73.06mm` (官方图纸) |
| `screen_w` / `screen_l` | LCD 可视区 | `43.20 / 57.60mm` (官方图纸) |
| `hole_edge` | 安装孔距边距离 (孔距 41×60) | `4.50mm` (官方图纸) |
| `pcb_screw_d` | 安装孔直径 | 按 M2.5 设计 — **图纸未标，需实测** |
| `glass_off_y` | 玻璃相对 PCB 的纵向偏移 | `-2.16mm` — **推算值，需实测** |
| `usb_edge_off` | USB-C 口在短边的位置 | 板宽一半 — **需实测** |

## 校准步骤

1. 开机，待机页会显示传感器实时状态 (`SENSOR: clear` / `BLOCKED`)
2. 用手挡一下光耦，确认状态会跳变 —— 不跳变说明接线或供电有问题
3. 点 `START` → 选人 → `START`，划 10 桨，记下屏幕距离
4. **长按标题栏 3 秒**进入管理员 → `Calibration`
5. 用 `-10 / -1 / +1 / +10` 调整（步进 0.000001 m/pulse），使：

   ```
   新值 = 当前值 × (实际距离 / 屏幕距离)
   ```

6. 点 `SAVE` 存进 NVS，断电不丢
