# Water Rower Monitor — 组装指南

> 划船机: Water Rower USA | SN: 132224
> MCU: ESP32-S3 DevKitC-1
> 屏幕: ST7735S 1.8" 128x160 RGB TFT (自带4按键 K1/K2/#/*)
> 传感器: 光敏电阻 LDR (原机自带, 4线 JST 插头)
> 音频: 椭圆喇叭 (非蜂鸣器)
> 载板: 大半板面包板 830孔 (165x55mm)
> 安装: 管插式 18mm (复用原机金属管)

---

## 第一步：准备材料

### 电子元件

| 序号 | 元件 | 数量 | 备注 |
|------|------|------|------|
| 1 | ESP32-S3 DevKitC-1 | 1 | USB-C |
| 2 | 1.8" TFT 128x160 自带4按键模块 | 1 | 就是你手上那块 |
| 3 | 大半板面包板 830孔 | 1 | 165x55mm |
| 4 | 原机光敏电阻+4线JST线缆 | 1 | 已有! 红蓝绿黑线 |
| 5 | 椭圆喇叭 (你手上那个) | 1 | 已有! 红黑线 |
| 6 | 5V 3000mAh USB-C 电池包 | 1 | 已有! 18650 蓝壳 |
| 7 | 短 USB-C 对 USB-C 线 (~15cm) | 1 | 电池→ESP32 |
| 8 | 杜邦线 公对公 | ~15根 | |
| 9 | USB-C 数据线 (长) | 1 | 烧录+充电 |
| 10 | M2.5 小螺丝 | 4 | 固定喇叭 |

### 3D 打印件 (CR-10S)

| STL | 说明 | 填充 | 支撑 |
|-----|------|------|------|
| `bottom_case.stl` | 底壳 (面包板+电池仓+喇叭) | 20% | 无 |
| `lid.stl` | 盖子 (TFT窗口+刻字) | 20% | 无 |
| `tube_socket.stl` | 18mm管座 (插原机金属管) | 40% | 无 |
| `battery_door.stl` | 电池盖 (滑入式, 换电池用) | 30% | 无 |

> 全部 0.2mm 层高, PLA 或 PETG

### 螺丝

| 螺丝 | 数量 | 用途 |
|------|------|------|
| M3x8mm | 4 | 盖子 ↔ 底壳 |
| M3x10mm | 2 | 管座 ↔ 底壳底部 |
| M4x25mm + 螺母 | 1 | 管座锁紧螺丝 |

---

## 第二步：导出 STL

1. 安装 OpenSCAD: `sudo apt install openscad`
2. 打开 `enclosure/enclosure.scad`
3. 分四次导出:

| 步骤 | 保留的代码 | 保存为 |
|------|-----------|--------|
| 1 | 只留 `bottom_case();` | `bottom_case.stl` |
| 2 | 只留 `lid();` (删 translate) | `lid.stl` |
| 3 | 只留 `tube_socket();` (删 translate) | `tube_socket.stl` |
| 4 | 只留 `battery_door();` (删 translate) | `battery_door.stl` |

每次: `F6` 渲染 → `F7` 导出

---

## 第三步：认识你的 TFT 模块

你的模块正面 (从照片看):

```
      K1      K2      K3/#    K4/*
    ┌─────┬─────┬──────┬──────┐
    │[btn]│[btn]│[btn] │[btn] │  ← 4个自带按键
    ├─────┴─────┴──────┴──────┤
    │                         │
    │      1.8" TFT 屏幕       │
    │      128 x 160          │
    │      RGB-TFT            │
    │                         │
    ├─────────────────────────┤
    │  ○                   ○  │  ← 螺丝孔
    └─────────────────────────┘
    ↑
   排针在这边 (左侧或底部, 看你的模块)
```

### 模块引脚 (翻到背面看丝印, 通常有这些脚):

```
VCC, GND, CS, RST, DC, SDA, SCL, LED, K1, K2, K3, K4
```

> **重要**: 拿到模块后, 看背面丝印确认每个引脚的顺序!
> 不同厂家的排列可能不同

---

## 第四步：面包板布局

```
面包板 165x55mm (从上方看)
┌──────────────────────────────────────────────────────────────┐
│  + 电源轨 (3.3V) ──────────────────────────────────────────  │
│  - 地线轨 (GND)  ──────────────────────────────────────────  │
│                                                              │
│  ┌──────────┐                          ┌──────────────────┐  │
│  │          │    跳线连接               │ TFT模块排针插这里 │  │
│  │ ESP32-S3 │  ←──────────────────────→│ (或用杜邦线连)   │  │
│  │          │                          └──────────────────┘  │
│  │          │                                                │
│  └──────────┘    LDR传感器线从这里引出 →  ──→ 右侧出线口      │
│                                                              │
│  - 地线轨 (GND)  ──────────────────────────────────────────  │
│  + 电源轨 (3.3V) ──────────────────────────────────────────  │
└──────────────────────────────────────────────────────────────┘
  ↑ USB-C 出线口                                    传感器出线口 ↑
```

- ESP32-S3 横跨中间沟槽, USB-C 朝左
- TFT 模块可以用排针直接插面包板, 或用杜邦线连

---

## 第五步：接线

### TFT 屏幕引脚 → ESP32-S3

```
TFT 模块引脚     跳线颜色(建议)    ESP32-S3
──────────────────────────────────────────
VCC              红色             3V3
GND              黑色             GND
CS               橙色             GPIO 10
RST              白色             GPIO 9
DC (A0)          绿色             GPIO 8
SDA (MOSI)       蓝色             GPIO 11
SCL (SCK)        黄色             GPIO 12
LED (背光)        紫色             GPIO 13
```

### TFT 模块自带按键 → ESP32-S3

```
模块按键引脚     功能              ESP32-S3
──────────────────────────────────────────
K1               UP (▲)           GPIO 5
K2               DOWN (▼)         GPIO 6
K3               # (停止/返回)     GPIO 7
K4               * (开始/恢复)     GPIO 15
```

> 按键的 GND 脚通常模块内部已经连好
> 如果你的模块按键没有内部下拉, 就把按键的公共脚连到 GND

### 原机 LDR 光敏电阻 (3线)

原机传感器只有 3 根线: VCC, Signal, GND

```
线缆颜色     功能                  ESP32-S3
──────────────────────────────────────────────
红色          VCC (传感器供电)      → 3.3V 电源轨
黑色          GND                  → GND 地线轨
信号线        LDR 模拟信号          → GPIO 1 (ADC)
```

> 如果不确定哪根是信号线:
> 用万用表量每根线对 GND 的电压, 遮住传感器时电压变化最大的就是信号线

### 喇叭

```
喇叭线           连接
──────────────────────────
红线 (+)          → GPIO 16
黑线 (-)          → GND
```

> 喇叭用螺丝固定在底壳内侧的 4 个安装柱上
> 喇叭正面朝下, 声音从底壳的音孔格栅出来

### 电源: 5V 3000mAh USB-C 电池包

```
电池包 (USB-C 输出) ──短USB-C线──→ ESP32-S3 (USB-C 输入)
```

- 电池放在底壳下方的电池仓里
- USB-C 线从电池仓穿到面包板区域，插 ESP32
- 换电池时，从背面滑出电池盖即可
- 充电时，把 USB-C 充电线从背面充电口插入电池包

### 电源轨 (别忘了!)

```
ESP32-S3 的 3V3  → 面包板 + 轨
ESP32-S3 的 GND  → 面包板 - 轨
```

---

## 第六步：配置软件

### 6.1 创建 GitHub 私有仓库

1. GitHub → New repository → 名称 `water-rower-logs` → **Private** → Create
2. 在仓库里创建 `workouts/` 文件夹 (添加一个 `.gitkeep` 文件)

### 6.2 生成 Token

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. 勾选 `repo` 权限 → Generate
3. 复制保存 (只显示一次)

### 6.3 修改 config.h

```cpp
// ===== 必须改 =====
#define WIFI_SSID     "你家WiFi名"
#define WIFI_PASSWORD "你家WiFi密码"
#define GITHUB_TOKEN  "ghp_你的token"
#define GITHUB_OWNER  "你的GitHub用户名"
#define GITHUB_REPO   "water-rower-logs"
```

### 6.4 确认按键引脚

**你的 TFT 模块按键引脚可能和默认不同!**

1. 看模块背面丝印, 找到 K1/K2/K3/K4 对应的引脚编号
2. 用万用表蜂鸣档测: 按下按键, 看哪个排针脚和 GND 通
3. 对应修改 `config.h`:

```cpp
#define BTN_UP_PIN    GPIO_NUM_?    // K1 对应的脚
#define BTN_DOWN_PIN  GPIO_NUM_?    // K2 对应的脚
#define BTN_HASH_PIN  GPIO_NUM_?    // K3/# 对应的脚
#define BTN_STAR_PIN  GPIO_NUM_?    // K4/* 对应的脚
```

---

## 第七步：烧录

```bash
cd ~/water-rower-monitor/firmware
pio run -t upload
pio device monitor        # 看调试输出
```

> ESP32-S3 烧录失败? 按住 BOOT → 插USB → 松开 → 重新 upload

---

## 第八步：组装

### 8.1 面包板装入底壳

```
侧面图:
┌─────────────────────────────┐  ← 盖子
│  ┌─── TFT 模块从窗口露出 ──┐│
│  │  [K1][K2][#][*]         ││  ← 按键朝上
│  │  ┌──────────────┐       ││
│  │  │   TFT 屏幕   │       ││
│  │  └──────────────┘       ││
│  └─────────────────────────┘│
│  ┌─────────────────────────┐│
│  │       面包板              ││  ← 面包板放在支撑台上
│  │   ESP32-S3 + 跳线        ││
│  └─────────────────────────┘│
└──────────────┬──────────────┘  ← 底壳
               │
         ┌─────┴─────┐
         │  管座      │          ← 管座螺丝固定在底壳底部
         │  ┌─────┐  │
         │  │金属管│  │          ← 原机的金属管插入管座
         │  │ 22mm│  │
         │  └─────┘  │
         │  锁紧螺丝→ ●          ← M4 螺丝锁紧
         └───────────┘
```

### 8.2 组装顺序

1. **面包板放入底壳** — 卡在四角支撑台上
2. **ESP32-S3 插面包板** — USB-C 朝左对准开口
3. **接好所有跳线** — 按第五步接线
4. **TFT 模块连线** — 排针插面包板或杜邦线连
5. **TFT 模块朝上** — 屏幕和按键面朝盖子方向
6. **盖盖子** — TFT 从窗口露出, 4 颗 M3 螺丝固定
7. **管座装底部** — 2 颗 M3 螺丝从底壳内侧拧入管座
8. **LDR 传感器 3 线从右侧槽口穿出**
9. **USB 线从左侧孔穿出**

### 8.3 安装到划船机

1. **量金属管直径** — 用卡尺量原机金属管外径
2. 如果不是 22mm, 改 `enclosure.scad` 里的 `tube_od = 你的数值`
3. 重新导出 `tube_socket.stl` 并打印
4. 把管座套在金属管上
5. 拧紧 M4 锁紧螺丝

### 8.4 连接原机 LDR 传感器

原机的光敏电阻传感器已经装好在飞轮位置了，你只需要把 4 线 JST 线缆连到面包板:

```
原机传感器结构 (已安装在飞轮旁):

    飞轮桨叶
    ┌───┐
    │   │  经过时遮住光线
    │   │         ↓
  [IR LED] ──光线──→ [光敏电阻 LDR]
    绿线供电              蓝线读信号

桨叶经过 → 光线被挡 → LDR 阻值变大 → 电压变化 → 检测到一个脉冲
```

- JST 线缆从外壳右侧的扁槽口穿入
- 4 根线分别插面包板对应位置
- **不需要额外传感器, 用原机自带的!**

### 8.5 安装喇叭

1. 喇叭正面 (锥面) 朝下
2. 对准底壳内侧的 4 个安装柱
3. 4 颗 M2.5 螺丝固定
4. 声音从底壳底部的格栅孔出来

---

## 第九步：校准

### 9.1 校准 LDR 阈值 (先做这个!)

1. 烧录固件后开机
2. **空闲页面会显示当前 LDR 值**: `LDR: 3200 (thr:2000)`
3. 用手遮住划船机飞轮传感器, 观察 LDR 值变化
4. 记下:
   - 不遮时 (亮) = 例如 3500
   - 遮住时 (暗) = 例如 800
5. 阈值设为中间: `LDR_THRESHOLD = (3500 + 800) / 2 = 2150`
6. 改 `config.h` → 重新烧录

### 9.2 校准距离

1. 按 `*` 开始训练
2. **慢慢均匀划 10 桨**
3. 按 `#` 暂停 → 记下距离
4. 按 `#` 结束
5. 计算:

```
新值 = 实际距离 / 屏幕显示距离 × 0.00875
```

6. 改 `config.h` 的 `METERS_PER_PULSE` → 重新烧录

---

## 操作说明

```
┌─────────────────────────────────────┐
│  TFT 模块按键:                       │
│  [K1/UP]  [K2/DN]  [K3/#]  [K4/*]  │
├──────┬──────────┬────────┬─────────┤
│ 按键 │   空闲   │ 训练中  │   暂停  │
├──────┼──────────┼────────┼─────────┤
│  *   │  开始    │  --    │  恢复   │
│  #   │  历史    │  暂停  │ 结束+存  │
│  UP  │  亮度+   │  --    │  --     │
│  DN  │  亮度-   │  --    │  --     │
├──────┼──────────┴────────┴─────────┤
│      │  历史查看:                    │
│  UP  │  上一条                      │
│  DN  │  下一条                      │
│  #   │  返回主页                    │
└──────┴─────────────────────────────┘
```

---

## 训练记录

自动上传到 `water-rower-logs` 仓库:

```
water-rower-logs/
└── workouts/
    ├── workout_20260402_183025.json
    └── workout_20260403_071512.json
```

JSON 内容:
```json
{
    "machine_sn": "132224",
    "machine_model": "Water Rower USA",
    "date": "2026-04-02T18:30:25Z",
    "duration_sec": 1800,
    "distance_m": 6500,
    "strokes": 450,
    "calories": 260,
    "avg_speed_ms": 3.61,
    "avg_stroke_rate": 15,
    "split_500m": "2:18"
}
```

---

## 故障排查

| 问题 | 解决 |
|------|------|
| 屏幕不亮 | 检查 VCC/GND, LED 背光脚 |
| 白屏 | DC/CS/RST 接线, 试 `INITR_GREENTAB` 或 `INITR_REDTAB` |
| 按键没反应 | 万用表测 K1-K4 脚, 确认 GPIO 编号对得上 |
| 距离不动 | 检查 LDR 线缆接对没, 空闲页看 LDR 值是否变化 |
| 喇叭没声 | 确认红线接 GPIO 16, 黑线接 GND |
| LDR 值不变 | 检查红线(VCC)和绿线(IR LED)是否供电 |
| WiFi 连不上 | 只支持 2.4GHz, 检查 SSID 密码 |
| 上传失败 | Token 过期 / 仓库不存在 / 没有 repo 权限 |
| 管座太松 | 拧紧 M4 锁紧螺丝, 或加一圈胶带增加摩擦 |
| 管座太紧 | 改 `tube_socket_id` 加大 0.5mm, 重新打印 |
| 烧录失败 | 按住 BOOT 键插 USB |

---

## 需要量的尺寸 (打印前必做!)

| 量什么 | 改哪里 | 当前默认值 |
|--------|--------|-----------|
| 原机金属管外径 | `enclosure.scad` → `tube_od` | 18mm (已确认) |
| 喇叭安装孔距(宽) | `enclosure.scad` → `spk_screw_spacing_w` | 48mm |
| 喇叭安装孔距(高) | `enclosure.scad` → `spk_screw_spacing_l` | 24mm |
| TFT 模块 PCB 总宽 | `enclosure.scad` → `tft_pcb_w` | 52mm |
| TFT 模块 PCB 总高 | `enclosure.scad` → `tft_pcb_l` | 40mm |
| TFT 可视区宽 | `enclosure.scad` → `tft_screen_w` | 29mm |
| TFT 可视区高 | `enclosure.scad` → `tft_screen_l` | 37mm |
| 按键条高度 | `enclosure.scad` → `tft_btn_strip_h` | 10mm |
