# Water Rower Monitor — 组装指南

> 划船机: Water Rower USA | SN: 132224
> 主板: Waveshare ESP32-S3-Touch-LCD-2.8 (屏幕/触摸/喇叭功放/充电 全部板载)
> 传感器: 原机光遮断器 Optek OPB365T55 (3 线)
> 供电: 外置 USB 充电宝 (2节 18650)，绑在划船机管子上
> 安装: 管插式 18mm (复用原机金属管)

**整机只需要接 3 根线**，其余全部板载。

---

## 第一步：准备材料

### 电子元件

| 序号 | 元件 | 数量 | 备注 |
|------|------|------|------|
| 1 | Waveshare ESP32-S3-Touch-LCD-2.8 | 1 | 16MB Flash + 8MB PSRAM |
| 2 | 原机光遮断器 + 3 线线缆 | 1 | 已有! 红/绿/蓝线 |
| 3 | 8Ω 2W 喇叭 | 1 | 板子包装里附带 |
| 4 | USB 充电宝 (2节 18650) | 1 | 已有! CK 18650-1234S-NP |
| 5 | 18650 电芯 | 2 | 充电宝盒里 |
| 6 | 短 USB-A → USB-C 线 | 1 | **直角头最好**，充电宝 → 板子 |
| 7 | USB-C 数据线 | 1 | 烧录用 |
| 8 | 杜邦线 母对母 | 3 | 传感器 → 12PIN 排针 |
| 9 | 魔术贴 / 扎带 | 2 | 把充电宝绑在划船机管子上 |

> 充电宝放在外壳**外面**。这样传感器的 5V 自动解决（VBus = USB 5V），
> 外壳也不用装电池，整机只有 18.7mm 厚，换电池充电都不用拆壳。

> ⚠️ 如果哪天不用充电宝、改接板载 MX1.25 电池座：**必须是 3.7V 单节 (1S)**。
> 7.4V 的 2S 电池会烧掉板载充电电路和主控，接反极性同样烧板。

### 3D 打印件

| STL | 说明 | 填充 | 支撑 |
|-----|------|------|------|
| `bottom_case.stl` | 底壳 (板卡槽 + 喇叭位 + 走线) | 20% | 无 |
| `lid.stl` | 盖子 (屏幕开窗 + 刻字) | 20% | 无 |
| `tube_socket.stl` | 18mm 管座 (插原机金属管) | 40% | 无 |

> 全部 0.2mm 层高, PLA 或 PETG。成品 74.9 × 94.0 × 18.7mm。
> 没有电池盖 —— 充电宝在外面。

### 螺丝

| 螺丝 | 数量 | 用途 |
|------|------|------|
| **M2.5x6mm 自攻** | **4** | **主板 ↔ 底壳内侧螺柱** |
| M3x10mm 自攻 | 4 | 盖子 ↔ 底壳四角柱 |
| M3x8mm 沉头 | 2 | 管座 ↔ 底壳底部 (盲孔) |
| M4x25mm + 螺母 | 1 | 管座锁紧螺丝 |

---

## 第二步：板子尺寸（已按官方图纸建模）

外壳的板卡尺寸全部读自 Waveshare 官方结构图，不是估的：

<https://files.waveshare.com/wiki/ESP32-S3-Touch-LCD-2.8/ESP32-S3-Touch-LCD-2.8_structure.pdf>

| 项目 | 数值 | 来源 |
|------|------|------|
| PCB 尺寸 | **49.90 × 69.00mm** | 图纸 |
| 触摸玻璃外形 (TP OD) | **50.54 × 73.06mm** | 图纸 |
| LCD 可视区 (AA) | **43.20 × 57.60mm** | 图纸 |
| 玻璃厚度 (TP+LCD) | **3.60mm** | 图纸 |
| 整板厚度 | **10.00mm** | 图纸 |
| 安装孔 | 4 个，距每边 **4.50mm**，孔距 **41.00 × 60.00mm** | 图纸 |
| 板背面元件高 | 4.80mm (= 10.00 − 3.60 − 1.60) | 推算 |

### ⚠️ 两个关键事实

**1. 玻璃比 PCB 大，四边都悬出。** 玻璃 73.06 比 PCB 69.00 长 4.06mm。
所以外壳里**任何地方都不能有高过 PCB 正面的墙**去夹板子边缘 —— 会顶到玻璃。
板子完全靠 4 个 M2.5 螺柱固定，边缘不受任何夹持。

**2. 所有连接器都在板子背面。** 正面被玻璃占满了，USB-C、12PIN 排针、
喇叭座全在背面。所以外壳的 USB 开口开在 PCB 下方，盖子不用开缺口。

### 仍需你确认的三项

| 量什么 | 改哪里 | 当前默认值 |
|--------|--------|-----------|
| 安装孔直径 | `pcb_screw_d` / `pcb_post_od` | 按 M2.5 自攻设计（图纸没标孔径） |
| 玻璃相对 PCB 的纵向位置 | `glass_off_y` | −2.16mm（由图纸 1.9mm 标注推算） |
| USB-C 在短边的位置 | `usb_edge_off` | 板宽一半 |

> `glass_off_y` 的推算依据：玻璃比 PCB 长 4.06mm，图纸在顶部标了 1.9mm，
> 余下 2.16mm 落在 USB-C 那一端。装之前把盖子比一下就知道对不对。

---

## 第三步：导出 STL

```bash
sudo apt install openscad
cd enclosure
openscad -o bottom_case.stl  -D "PART=1" enclosure.scad
openscad -o lid.stl          -D "PART=2" enclosure.scad
openscad -o tube_socket.stl  -D "PART=3" enclosure.scad
```

也可以在 OpenSCAD 图形界面里改文件末尾的 `PART = 0;`，然后 `F6` 渲染 → `F7` 导出。
`PART = 0` 是三件套的预览排布。

---

## 第四步：接线 (只有 3 根)

板子对外只有一排 12PIN 排针，**可自由使用的只有 IO18 和 IO15**，
其余是 USB D±、板载 I2C 和 UART0。本固件用 IO18。

```
原机传感器线     →   12PIN 排针        说明
─────────────────────────────────────────────────
红 (VCC)         →   VBus (5V)        !! 必须 5V, 3.3V 点不亮 IR LED !!
绿 (GND)         →   GND
蓝 (SIG)         →   IO18             数字信号, 遮光 = LOW
```

原机传感器的工作方式：

```
    飞轮桨叶
    ┌───┐
    │   │  经过时挡住红外光
    │   │         ↓
  [IR LED] ──红外──→ [光电管]
    5V 供电              信号线

桨叶经过 → 光被挡 → SIG 拉低 → 记一个脉冲 (60 齿 = 每转 60 个脉冲)
```

> 光电管是开集电极输出，固件用 `INPUT_PULLUP` 上拉到 3.3V，
> 所以传感器用 5V 供电也不会把 5V 灌进 GPIO。

### 传感器的 5V 从哪来

`VBus` 就是 USB-C 进来的 5V。用外置充电宝供电，**传感器的 5V 自动就有了**，
不用升压板，也不用换传感器。

只有改接板载 MX1.25 锂电池时才会缺 5V —— 那时要么加 3.7V→5V 升压小板，
要么改用霍尔传感器（`config.h` 换成 `SENSOR_TYPE_HALL`，同样接 IO18，3.3V 可用）。

---

## 第五步：配置软件

```bash
cp firmware/include/config.h.example firmware/include/config.h
```

编辑 `firmware/include/config.h`：

```cpp
// ===== 必须改 =====
#define WIFI_COUNT 2
#define WIFI_SSIDS     {"你家WiFi", "备用WiFi"}
#define WIFI_PASSWORDS {"密码1", "密码2"}

#define NAS_HOST    "192.168.1.100"          // TrueNAS 地址
#define NAS_API_KEY "1-你的APIKey"
#define NAS_PATH    "/mnt/pool/water_rower_records"

#define USER_COUNT 2
#define USER_NAMES {"Alice", "Bob"}
#define MACHINE_SN "132224"

// ===== 传感器类型, 二选一 =====
#define SENSOR_TYPE_BLOCKER      // 原机光遮断器
// #define SENSOR_TYPE_HALL      // 霍尔 + 磁铁
```

**引脚不用改** —— 屏幕、触摸、喇叭全是板载固定连接，
`config.h` 里那些定义只是告诉驱动它们在哪。

在 TrueNAS 上建好 `NAS_PATH` 指向的目录，并生成一个有写权限的 API Key。

---

## 第六步：烧录

```bash
cd firmware
pio run -t upload
pio device monitor
```

> USB-C 直连 ESP32-S3 的原生 USB（没有 USB-UART 芯片），串口走 USB CDC。
> 烧录失败：按住 BOOT → 插 USB → 松开 BOOT → 重新 upload。

---

## 第七步：组装

### 7.1 装配顺序

```
剖面图 (从侧面看):

   ┌──────────────────────────────┐  ← 盖子 (屏幕从窗口露出, 与盖面齐平)
   │  ┌────────────────────────┐  │
   │  │   触摸玻璃 / 屏幕       │  │
   │  ├────────────────────────┤  │  ← 玻璃比 PCB 大, 悬在边缘外
   │  │   主板 PCB (4 螺柱托住) │  │
   │  │   背面元件 ↓            │  │
   │  ├────────────────────────┤  │
   │  │   喇叭 (卡在限位墙里)   │  │  ← 6mm 薄层, 只放喇叭和走线
   └──┴────────────────────────┴──┘  ← 底壳
              │           └──USB-C 线出去 → 充电宝 (外挂)
        ┌─────┴─────┐
        │   管座     │                ← 2 颗 M3 沉头从底壳外面拧入
        │  ┌─────┐  │
        │  │金属管│  │                ← 原机 18mm 金属管插入
        │  └─────┘  │
        │  锁紧螺丝→ ●                ← M4 螺丝夹紧
        └───────────┘
```

1. **喇叭放进底壳** — 卡在仓内的四条限位墙之间，锥面朝下对着底部格栅孔，
   接到板子的喇叭座
2. **传感器 3 根杜邦线** — 从底壳侧面的扁槽口穿进来，接 12PIN 排针（在板子背面）
3. **主板装上 4 个螺柱** — 屏幕朝上，4 颗 M2.5 自攻拧进内侧螺柱。
   **不要试图把板子塞进什么槽里** —— 玻璃比 PCB 大，边缘不能受夹持
4. **插 USB-C 供电线** — 从底边开口进来插板子背面的口，
   **建议直角头**，减小开口处受力
5. **盖盖子** — 屏幕玻璃穿过开窗，4 颗 M3 自攻拧进四角柱
6. **装管座** — 2 颗 M3 沉头螺丝从底壳外面拧入盲孔
7. **充电宝绑在管子上** — 魔术贴或扎带固定在划船机金属管上，
   位置以 USB 线不被拉扯为准

### 7.2 安装到划船机

1. 用卡尺量原机金属管外径（默认按 18mm 建模，已确认）
2. 不是 18mm 就改 `enclosure.scad` 的 `tube_od`，重新导出打印
3. 管座套上金属管，拧紧 M4 锁紧螺丝
4. 管座太松 → 拧紧 M4 或缠一圈胶带；太紧 → `tube_socket_id` 加 0.5mm 重打

---

## 第八步：首次开机自检

开机会依次出现：波浪动画 → 划船小人 → `WATER ROWER v3.0 touch edition`。

| 现象 | 说明 |
|------|------|
| 屏幕亮，显示待机页 | 正常 |
| 屏幕亮但点哪都没反应 | 触摸没起来，看串口 `[TOUCH] CST328 ...` |
| 开机闪 `TOUCH INIT FAILED` | CST328 排线/I2C 有问题 |
| 有开机提示音 | I2S 喇叭正常（音量还可以拧板上的旋钮） |
| 待机页显示 `SENSOR: clear` | 传感器连上了 |

**传感器自检**：用手挡住飞轮旁的光遮断器，待机页应该在
`SENSOR: clear` 和 `SENSOR: BLOCKED` 之间跳变。不跳变就是接线或 5V 供电有问题。

### ⚠️ 充电宝自动关断测试（必做）

很多充电宝在输出电流低于 50-70mA 时会自动断电省电。本机约 150-250mA，
正常不会断，但**一定要实测确认**：

1. 拔掉烧录用的数据线，只接充电宝
2. 待机页把亮度调到**最低**（电流最小的工况）
3. 放着不管 15-20 分钟

中途自己黑屏 = 充电宝断了。解决办法：在 12PIN 的 `VBus` 和 `GND` 之间
并一个 **100Ω 1W 电阻**（约 50mA 假负载），代价是续航缩短。

顺便确认充电宝输出是 5V/1A 还是 2A —— 1A 够用，但开机 WiFi 扫描有电流尖峰，
劣质 1A 板子可能掉压重启。

---

## 第九步：校准距离

1. 点 `START` → 选人 → `START`
2. **慢慢均匀划 10 桨**，记下屏幕显示的距离
3. 点 `PAUSE` → `FINISH`
4. 回到待机页，**长按顶部标题栏 3 秒**进入管理员 → `Calibration`
5. 用 `-10 / -1 / +1 / +10` 调整（步进 0.000001 m/pulse）：

   ```
   新值 = 当前值 × (实际距离 / 屏幕距离)
   ```

6. 点 `SAVE` — 存进 NVS，断电不丢，不用重新烧录

---

## 操作说明 (全触摸)

板上只有 BOOT 和 RESET 两个键，跟操作无关。

| 界面 | 操作 |
|------|------|
| 待机 | `START` 开始 · `HISTORY` 历史 · `−` / `+` 调亮度 |
| 待机 | **长按顶部标题栏 3 秒** → 管理员 |
| 选人 | 点名字选中 → `START` 开始；上下滑动翻页 |
| 划船中 | `PAUSE` 暂停 |
| 暂停 | `RESUME` 继续 · `FINISH` 结束并上传 |
| 总结 | `DONE` 返回 |
| 历史 | `PREV` / `NEXT` 翻页，或上下滑动 |
| 管理员 | `Calibration` 距离标定 · `User Weights` 体重 · `Password` 改密码 |
| 密码输入 | 屏幕数字键盘 0-9，`DEL` 退格，`ESC` 取消 |

> 选 `Guest` 的训练只显示和蓝牙广播，不上传 NAS。

---

## 训练记录

每次训练存一个 JSON 到 TrueNAS：

```
/mnt/pool/water_rower_records/
├── Alice_20260402_183025.json
└── Bob_20260403_071512.json
```

```json
{
    "machine_sn": "132224",
    "machine_model": "Water Rower USA",
    "date": "2026-04-02T18:30:25Z",
    "user": "Alice",
    "duration_sec": 1800,
    "distance_m": 6500,
    "strokes": 450,
    "calories": 260,
    "avg_speed_ms": 3.61,
    "avg_stroke_rate": 15,
    "split_500m": "2:18"
}
```

同时以 BLE FTMS 广播，可以直接连 Zwift / Kinomap / EXR 等划船 App。

---

## 故障排查

| 问题 | 解决 |
|------|------|
| 屏幕全黑 | 背光是 GPIO5，先看串口有没有输出；确认板子没进下载模式 |
| 点击位置偏了/镜像 | 改 `config.h` 的 `TOUCH_MIRROR_X` / `TOUCH_MIRROR_Y` / `TOUCH_SWAP_XY` |
| 触摸完全没反应 | 串口看 `[TOUCH] CST328 id` —— 不是 `0xCACA` 就是 I2C 没通 |
| 距离不动 | 待机页看 `SENSOR:` 会不会跳变；不跳就查 5V 和信号线 |
| 放着不用会自己黑屏 | 充电宝自动关断了 —— `VBus`/`GND` 并 100Ω 1W 假负载 |
| 开机就重启 / 掉压 | 充电宝输出电流不够，换 2A 输出的 |
| 充电宝插着但没反应 | 有些充电宝要按一下按钮才输出 |
| 喇叭没声 | 拧板上的音量旋钮；确认喇叭插在板载喇叭座上 |
| 提示音有爆音 | 调低 `config.h` 的 `TONE_VOLUME` |
| WiFi 连不上 | 只支持 2.4GHz，检查 SSID / 密码 |
| 上传失败 | API Key 权限、`NAS_PATH` 目录是否存在、NAS 是否同网段 |
| 编译报 ADC 错误 | 用了 `SENSOR_TYPE_LDR` —— 这块板的可用引脚都是 ADC2，跟 WiFi 冲突 |
| 烧录失败 | 按住 BOOT 键插 USB |
| 管座太松/太紧 | 拧紧 M4 / 改 `tube_socket_id` |
| 盖子压到玻璃 / 屏幕对不准窗口 | `glass_off_y` 推算值不对，量实物后改，重打盖子 |
| 板子螺丝孔对不上螺柱 | 孔距应为 41×60mm；对不上就是板子版本不同，改 `hole_edge` |
| M2.5 螺丝太松 | 板子可能是 M3 孔，把 `pcb_screw_d` 改 2.6、`pcb_post_od` 改 6 |
